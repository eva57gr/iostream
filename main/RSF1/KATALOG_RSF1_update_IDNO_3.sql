SELECT CUIIO, USER_CHANGE, LOG_DATA_REG
FROM (
  SELECT
    CUIIO,
    USER_CHANGE,
    LOG_DATA_REG,
    ROW_NUMBER() OVER (PARTITION BY CUIIO ORDER BY LOG_DATA_REG DESC) AS rn
  FROM
    CIS2.RENIM_LOG
    
     WHERE 
CUIIO IN (
77014
)
)
WHERE rn = 1;